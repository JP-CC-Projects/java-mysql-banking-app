# Main Database Configuration (MySQL)
# Uncomment these lines if you want to switch to MySQL for live
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/hibernate_example?serverTimezone=UTC
spring.datasource.username=example_user
spring.datasource.password=P@ssword123

# Test Database Configuration (H2)
# Uncomment these lines if you want to switch to H2 for testing
#spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
#spring.datasource.driver-class-name=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=

# JPA Configuration
# Adjust this line for create-drop behavior
spring.jpa.hibernate.ddl-auto=create-drop
#spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Thymeleaf Configuration
spring.thymeleaf.cache=false
